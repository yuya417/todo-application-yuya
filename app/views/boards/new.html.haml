.container
  %h2.title-text
    New Board
  = form_with(model: @board, local: true) do |f|
    %div.name-text
      = f.label :title, 'Name'
    %div
      = f.text_field :title,class: 'text'
    %div.name-text
      = f.label :content, 'Description'
    %div
      = f.text_area :content
    = f.submit 'Submit'